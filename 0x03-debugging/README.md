Folder for tasks on Debugging.

0: MULTIPLE MAINS
Create a file called 0-main.c based on the main.c file given in the task.
This file must test that the function positive_or_negative() gives the correct output when given a case of 0.

1: LIKE, COMMENT, SUBSCRIBE
Copy the main file; and comment out the part of the code that is causing the output to go into an infinite loop.

2: 0 > 972?
Given a program that prints the largest of three integers, fix the code so that it correctly prints out the largest of three numbers, no matter the case.

3: LEAP YEAR
Given a program converts a date to the day of year and determines how many days are left in the year, taking leap year into consideration; fix the print_remaining_days() function so that the output works correctly for all dates and all leap years.
