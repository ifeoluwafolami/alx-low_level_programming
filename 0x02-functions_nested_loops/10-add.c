#include "main.h"

/**
 * add -returns the sum of two integers
 * @m: number being added
 * @n: number being added
 * Return: Value of sum
 */
int add(int m, int n)
{
	int sum;

	sum = m + n;
	return (sum);
}
