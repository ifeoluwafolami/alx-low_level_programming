#include "main.h"
#include <stdio.h>
/**
 * main - prints program's name
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0
 */
int main(int __attribute((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
